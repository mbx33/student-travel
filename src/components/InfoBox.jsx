import React from 'react';
import styled from 'styled-components';

const InfoBox = () => {
	return <Wrapper>InfoBox</Wrapper>;
};

export default InfoBox;

const Wrapper = styled.article`
	width: 50vw;
	height: 60vh;
	background-color: var(--secondary-color);
	border-radius: 1rem;
`;
